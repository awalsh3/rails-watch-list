<h1> Create a new list </h1>

<%= simple_form_for(@list) do |f| %>
  <%=f.input :name, label: "Enter the name of your list:" %>
  <%= f.button :submit, "Submit", class: "btn btn-primary", "data-disable-with" => "Submitting.." %>
<% end %>
